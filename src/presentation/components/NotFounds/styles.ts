import { StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  containerNoEvents: {
    display: 'flex',
    justifyContent: 'center',
    alignItems: 'center',
    minHeight: 400,
  },
  contentNoEvents: {
    justifyContent: 'center',
    alignItems: 'center',
  },
  ImageNoEvents: {
    height: 230,
    width: 230,
    objectFit:'cover'
  },
  textNoEvents: {
    paddingVertical: 16,
    gap: 16,
    alignItems: 'center',
  },
});
