import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  containerStyles: {
    height: 10,
    width: '100%',
    backgroundColor: '#F2F7FF',
    borderRadius: 50,
  },
  fillerStyles: {
    height: '100%',
    borderRadius: 50,
  },
  labelStyles: {
    padding: 5,
    color: 'white',
    fontWeight: 'bold',
  },
});
