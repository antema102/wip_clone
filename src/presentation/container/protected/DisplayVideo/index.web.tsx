import React, {useState, useRef, useEffect} from 'react';
import {StyleSheet, Text, View, Modal, Platform} from 'react-native';

export const DisplayVideo = (props: any) => {
  return (
    <View style={{flex: 1, marginTop: Platform.OS == 'ios' ? 25 : 0}}>
    </View>
  );
};

