import React, { useState, useRef, useEffect } from 'react';
import { Dialog } from 'primereact/dialog';

export const DisplayVideo = (props: any) => {
  return (
    <div style={{ flex: 1, marginTop: Platform.OS == 'ios' ? 25 : 0 }}></div>
  );
};
