import React, {useEffect, useState} from 'react';
import {
  View,
  RefreshControl,
  Text,
  Image,
  Platform,
  TouchableOpacity,
} from 'react-native';

const EnterpriseOfferSheet = (props: any) => {
  return (
    <View style={{marginBottom: 0}}>
    </View>
  );
};

export const EnterpriseOfferSheetScreen: any = EnterpriseOfferSheet;
