import icons from './icons';
import images from './images';
import { COLORS, SIZES, FONTS } from './theme';

const theme = { COLORS, SIZES, FONTS };

export { icons, images, COLORS, SIZES, FONTS, theme };
